# Batch122 — Universal Verify Kit & Single Release Flow
Priority: P0 Runtime/Build/Release Flow simplification.

Adds stable universal entrypoints: `npm run verify:all`, `npm run verify:release`, and `APP_URL=https://your-vercel-url.vercel.app npm run verify:release`.

No AI, no payment, no marketplace, no fake URL, no product logic change, no production-ready claim without deployed URL smoke proof.
