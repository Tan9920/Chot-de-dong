export function analyzeAcademicTrace(...args: any[]) { const input = args.length === 1 && typeof args[0] === 'object' ? args[0] : args[1] || {}; const sourceMeta = input.sourceMeta || { status: input.sourceStatus || 'seed', sourceLabel: input.sourceLabel || 'Seed/scaffold trace only', packId: input.packId || 'demo-pack', references: input.references || [] }; return { sourceStatus: sourceMeta.status || input.sourceStatus || 'seed', sourceMeta, referenceCount: Number(input.referenceCount ?? sourceMeta.references?.length ?? 0), fieldEvidenceCount: Number(input.fieldEvidenceCount ?? 0), conflicts: Array.isArray(input.conflicts) ? input.conflicts : [], warnings: ['Seed/scaffold trace only.'] }; }
