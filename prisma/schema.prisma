generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model DemoRuntimeNotice {
  id        String   @id @default(cuid())
  note      String
  createdAt DateTime @default(now())
}
